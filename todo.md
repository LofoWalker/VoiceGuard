# VoiceGuard — Next Generation Rules Backlog

## VG-017 — Implement MicroPauseDistributionRule

Goal:
Detect unnatural pause distributions and overly regular conversational pacing typical of TTS systems.

Tasks:
- Create `MicroPauseDistributionRule`
- Detect:
    - micro silences
    - hesitation gaps
    - inter-word pause variance
    - rhythm regularity
- Compute:
    - pause histogram
    - pause variance
    - silence density
- Add confidence scaling based on available speech duration
- Exclude chunks with insufficient speech activity
- Integrate into `validateEngine`
- Add per-rule metrics reporting

Acceptance Criteria:
- AUC >= 0.65
- Low correlation with SpectralArtifactsRule
- No significant latency increase

------------------------------------------------------------

## VG-018 — Implement EmotionalVarianceRule

Goal:
Detect lack of natural emotional fluctuations in synthetic speech.

Tasks:
- Create `EmotionalVarianceRule`
- Analyze:
    - pitch variance
    - intensity fluctuations
    - emotional contour stability
    - speaking energy evolution
- Compute temporal emotional variability score
- Add confidence adaptation based on speech length
- Ignore low-volume segments
- Integrate into benchmark pipeline

Acceptance Criteria:
- AUC >= 0.65
- Improves recall without increasing FPR significantly
- Stable across rerecorded datasets

------------------------------------------------------------

## VG-019 — Implement TurnTakingLatencyVarianceRule

Goal:
Detect suspiciously stable conversational response latencies.

Tasks:
- Create `TurnTakingLatencyVarianceRule`
- Detect:
    - speaker switch events
    - response timing consistency
    - repeated latency patterns
- Measure:
    - mean latency
    - latency variance
    - rolling latency consistency
- Store timing history in `ConversationContext`
- Ignore insufficient interaction samples
- Add configurable minimum switch count

Acceptance Criteria:
- Detects repetitive AI response timings
- Low false positive rate
- AUC >= 0.75 on conversational datasets

------------------------------------------------------------

## VG-020 — Implement HarmonicConsistencyRule

Goal:
Detect unnaturally stable harmonic structures generated by vocoders.

Tasks:
- Create `HarmonicConsistencyRule`
- Analyze:
    - harmonic stability
    - spectral coherence
    - harmonic drift
    - frequency micro-instabilities
- Compute harmonic consistency score
- Normalize score against human baseline
- Add chunk quality filtering

Acceptance Criteria:
- AUC >= 0.70
- Complementary to SpectralArtifactsRule
- Minimal CPU usage

------------------------------------------------------------

## VG-021 — Implement EnergyEnvelopeRule

Goal:
Detect overly controlled or artificial speech energy envelopes.

Tasks:
- Create `EnergyEnvelopeRule`
- Analyze:
    - RMS evolution
    - attack/release curves
    - dynamic range consistency
    - abrupt energy transitions
- Detect synthetic smoothness patterns
- Add temporal smoothing

Acceptance Criteria:
- AUC >= 0.60
- Low correlation with ProsodicDynamicsRule
- Stable across noisy recordings

------------------------------------------------------------

## VG-022 — Implement RoomResponseRule

Goal:
Detect absence of realistic room acoustics or artificial reverberation signatures.

Tasks:
- Create `RoomResponseRule`
- Analyze:
    - early reflections
    - reverb decay
    - room resonance patterns
    - acoustic depth consistency
- Detect overly dry or synthetic environments
- Add fallback for noisy recordings

Acceptance Criteria:
- AUC >= 0.70
- Strong complementarity with NoiseLinearityRule
- Robust on phone-recorded datasets

------------------------------------------------------------

## VG-023 — Implement CodecArtifactRule

Goal:
Detect artifacts caused by TTS + VoIP + codec pipelines.

Tasks:
- Create `CodecArtifactRule`
- Detect:
    - GSM artifacts
    - packet smoothing
    - transcoding fingerprints
    - codec-induced spectral anomalies
- Build feature extraction pipeline
- Add confidence scaling based on bandwidth quality
- Test on WhatsApp / VoIP rerecorded samples

Acceptance Criteria:
- Strong performance on phone-call datasets
- AUC >= 0.75
- Minimal impact on latency budget

------------------------------------------------------------

## VG-024 — Implement HumanImperfectionRule

Goal:
Detect absence of natural human speech imperfections.

Tasks:
- Create `HumanImperfectionRule`
- Detect:
    - hesitations
    - stutters
    - syllable truncation
    - vocal fry
    - spontaneous corrections
- Build imperfection scoring model
- Add temporal accumulation logic

Acceptance Criteria:
- Improves human detection precision
- Reduces false positives
- Works on low-quality microphones

------------------------------------------------------------

## VG-025 — Implement SpeechEntropyRule

Goal:
Measure entropy differences between human and synthetic speech.

Tasks:
- Create `SpeechEntropyRule`
- Analyze:
    - spectral entropy
    - temporal entropy
    - signal compressibility
    - phonetic variability
- Compute rolling entropy score
- Add normalization against chunk duration

Acceptance Criteria:
- AUC >= 0.65
- Low overlap with existing spectral rules
- Stable on rerecorded datasets

------------------------------------------------------------

## VG-026 — Add Rule Correlation Analyzer

Goal:
Detect redundant or highly correlated rules.

Tasks:
- Create `RuleCorrelationAnalyzer`
- Compute:
    - Pearson correlation
    - covariance matrix
    - mutual information
- Generate correlation report in `validateEngine`
- Highlight redundant rules automatically
- Export correlation heatmap data

Acceptance Criteria:
- Correlation report available after validation
- Helps identify duplicate signal sources
- No noticeable performance regression

------------------------------------------------------------

## VG-027 — Add Score Calibration Layer

Goal:
Transform raw rule scores into calibrated probabilities.

Tasks:
- Implement calibration pipeline
- Experiment with:
    - sigmoid calibration
    - Platt scaling
    - isotonic regression
- Add calibration stage before final aggregation
- Compare calibrated vs raw outputs
- Add validation metrics:
    - Brier score
    - calibration error

Acceptance Criteria:
- More stable thresholds
- Reduced FPR variance
- Better probability realism

------------------------------------------------------------

## VG-028 — Build Real Phone Call Dataset

Goal:
Create a realistic evaluation dataset matching real-world scam conditions.

Tasks:
- Build rerecording pipeline:
    - TTS
    - speaker playback
    - smartphone mic capture
    - VoIP/GSM transmission
- Create:
    - human call samples
    - AI call samples
    - noisy environments
    - cheap microphone recordings
- Integrate into benchmark suite
- Separate metrics by dataset type

Acceptance Criteria:
- New realistic dataset available
- Dedicated benchmark mode
- Improved real-world evaluation quality

------------------------------------------------------------

## VG-029 — Improve Aggregator Robustness

Goal:
Prevent strong rules from dominating the final decision.

Tasks:
- Add per-rule contribution caps
- Add score normalization
- Add nonlinear score compression
- Prevent correlated rules from double-counting
- Add configurable human safety bias
- Compare aggregation strategies

Acceptance Criteria:
- Lower false positive rate
- More stable global scores
- Better balance between recall and precision